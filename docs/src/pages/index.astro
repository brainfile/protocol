---
import logo from '../assets/logo.png';
---

<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Brainfile - A task file format for developers working with AI agents</title>
  <meta name="description" content="YAML structure + Markdown content. Git-friendly. AI agents can read and modify it.">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      border-radius: 0 !important;
    }

    :root {
      --bg: #000000;
      --text: #FFFFFF;
      --text-muted: rgba(255, 255, 255, 0.6);
      --accent: #FFFFFF;
      --border: rgba(255, 255, 255, 0.08);
      --code-bg: #0A0A0A;
      --space-xs: 8px;
      --space-sm: 16px;
      --space-md: 24px;
      --space-lg: 32px;
      --space-xl: 48px;
      --space-2xl: 64px;
      --space-3xl: 80px;
    }

    html[data-theme="light"] {
      --bg: #FFFFFF;
      --text: #000000;
      --text-muted: rgba(0, 0, 0, 0.6);
      --accent: #000000;
      --border: rgba(0, 0, 0, 0.08);
      --code-bg: #FAFAFA;
    }

    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
      background: var(--bg);
      color: var(--text);
      line-height: 1.6;
      letter-spacing: -0.01em;
      font-size: 15px;
    }

    ::selection {
      background: rgba(255, 255, 255, 0.1);
    }

    html[data-theme="light"] ::selection {
      background: rgba(0, 0, 0, 0.1);
    }

    /* Navigation */
    nav {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 100;
      background: rgba(0, 0, 0, 0.8);
      backdrop-filter: blur(10px);
      border-bottom: 1px solid var(--border);
      height: 56px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 var(--space-lg);
    }

    html[data-theme="light"] nav {
      background: rgba(255, 255, 255, 0.9);
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 8px;
      text-decoration: none;
      color: var(--text);
    }

    .logo img {
      width: 32px;
      height: 32px;
    }

    .logo-text {
      font-weight: 600;
      font-size: 1.25rem;
      letter-spacing: -0.02em;
    }

    .nav-links {
      display: flex;
      gap: var(--space-md);
      align-items: center;
    }

    .nav-links a {
      color: var(--text-muted);
      text-decoration: none;
      font-size: 0.9375rem;
      transition: all 0.2s;
      padding: var(--space-xs) var(--space-sm);
      border: 1px solid transparent;
    }

    .nav-links a:hover {
      color: var(--text);
      border-color: var(--border);
    }

    .theme-toggle {
      background: none;
      border: 1px solid var(--border);
      color: var(--text);
      padding: var(--space-xs);
      cursor: pointer;
      transition: all 0.2s;
      display: flex;
      align-items: center;
    }

    .theme-toggle:hover {
      border-color: var(--accent);
    }

    .theme-toggle svg {
      width: 18px;
      height: 18px;
    }

    /* Main Content */
    main {
      max-width: 1200px;
      margin: 0 auto;
      padding: calc(56px + var(--space-3xl)) var(--space-lg) var(--space-3xl);
    }

    h1 {
      font-size: 2.5rem;
      font-weight: 700;
      letter-spacing: -0.04em;
      line-height: 1.2;
      margin-bottom: var(--space-sm);
    }

    .tagline {
      font-size: 1.125rem;
      color: var(--text-muted);
      margin-bottom: var(--space-3xl);
      line-height: 1.6;
    }

    /* Hero Grid: Code + Tools */
    .hero-grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: var(--space-lg);
      margin-bottom: var(--space-xl);
    }

    @media (min-width: 900px) {
      .hero-grid {
        grid-template-columns: 1.5fr 1fr;
      }
    }

    /* Code Block */
    .code-block {
      background: var(--code-bg);
      border: 1px solid var(--border);
      padding: var(--space-md);
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.875rem;
      line-height: 1.7;
      overflow-x: auto;
      height: fit-content;
    }

    .code-header {
      color: var(--text-muted);
      margin-bottom: var(--space-sm);
      font-size: 0.8125rem;
    }

    .code-content {
      color: var(--text);
    }

    .code-comment {
      color: var(--text-muted);
    }

    /* Tool Cards */
    .tool-cards {
      display: flex;
      flex-direction: column;
      gap: var(--space-sm);
    }

    .tool-card {
      border: 1px solid var(--border);
      padding: var(--space-md);
      transition: all 0.2s;
      text-decoration: none;
      color: var(--text);
      display: block;
      background: transparent;
    }

    .tool-card:hover {
      border-color: var(--accent);
      transform: translateX(2px);
    }

    .tool-card-title {
      font-weight: 600;
      font-size: 1rem;
      margin-bottom: var(--space-xs);
      letter-spacing: -0.02em;
    }

    .tool-card-package {
      font-family: 'JetBrains Mono', monospace;
      font-size: 0.8125rem;
      color: var(--text-muted);
      margin-bottom: var(--space-xs);
    }

    .tool-card-description {
      font-size: 0.875rem;
      color: var(--text-muted);
      line-height: 1.5;
    }

    /* Feature List */
    .feature-list {
      list-style: none;
      padding: 0;
      margin-bottom: var(--space-3xl);
      display: grid;
      gap: var(--space-xs);
    }

    .feature-list li {
      padding-left: var(--space-md);
      position: relative;
      color: var(--text-muted);
    }

    .feature-list li:before {
      content: '─';
      position: absolute;
      left: 0;
      color: var(--text);
    }

    /* Section */
    section {
      margin-bottom: var(--space-3xl);
    }

    h2 {
      font-size: 1.5rem;
      font-weight: 600;
      letter-spacing: -0.03em;
      margin-bottom: var(--space-md);
      text-transform: uppercase;
      font-size: 0.875rem;
      color: var(--text-muted);
      font-weight: 500;
    }

    p {
      color: var(--text-muted);
      margin-bottom: var(--space-md);
      line-height: 1.7;
    }

    /* Problem/Solution Grid */
    .two-col {
      display: grid;
      gap: var(--space-lg);
      margin-bottom: var(--space-3xl);
    }

    @media (min-width: 768px) {
      .two-col {
        grid-template-columns: 1fr 1fr;
      }
    }

    .col-header {
      font-weight: 600;
      margin-bottom: var(--space-sm);
      color: var(--text);
    }

    ul {
      list-style: none;
      padding: 0;
    }

    ul li {
      padding-left: var(--space-md);
      position: relative;
      margin-bottom: var(--space-xs);
      color: var(--text-muted);
    }

    ul li:before {
      content: '·';
      position: absolute;
      left: 0;
      color: var(--text);
    }

    /* Footer Statement */
    .footer-statement {
      text-align: center;
      padding: var(--space-xl) 0;
      color: var(--text-muted);
      font-size: 0.875rem;
      border-top: 1px solid var(--border);
      margin-top: var(--space-3xl);
    }

    /* Links */
    a:not(.logo):not(.tool-card):not(.nav-links a) {
      color: var(--text);
      text-decoration: underline;
      text-decoration-color: var(--text-muted);
      text-underline-offset: 2px;
      transition: all 0.2s;
    }

    a:not(.logo):not(.tool-card):not(.nav-links a):hover {
      text-decoration-thickness: 2px;
    }

    /* Mobile Responsive */
    @media (max-width: 900px) {
      h1 {
        font-size: 2rem;
      }

      main {
        padding: calc(56px + var(--space-2xl)) var(--space-md) var(--space-2xl);
      }

      .nav-links a span {
        display: none;
      }

      .hero-grid {
        grid-template-columns: 1fr;
      }

      .tool-cards {
        order: -1; /* Show tools above code on mobile */
      }
    }

    @media (max-width: 640px) {
      nav {
        padding: 0 var(--space-md);
      }

      .nav-links {
        gap: var(--space-sm);
      }
    }
  </style>
</head>
<body>
  <nav>
    <a href="/" class="logo">
      <img src={logo.src} alt="Brainfile">
      <span class="logo-text">Brainfile</span>
    </a>
    <div class="nav-links">
      <a href="/getting-started/quick-start"><span>Docs</span></a>
      <a href="/protocol/specification"><span>Spec</span></a>
      <a href="https://github.com/brainfile" target="_blank"><span>GitHub</span></a>
      <button class="theme-toggle" id="themeToggle" title="Toggle theme">
        <svg class="sun-icon" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="12" cy="12" r="5"/>
          <line x1="12" y1="1" x2="12" y2="3"/>
          <line x1="12" y1="21" x2="12" y2="23"/>
          <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/>
          <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
          <line x1="1" y1="12" x2="3" y2="12"/>
          <line x1="21" y1="12" x2="23" y2="12"/>
          <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/>
          <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
        </svg>
        <svg class="moon-icon" style="display: none;" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/>
        </svg>
      </button>
    </div>
  </nav>

  <main>
    <h1>Brainfile</h1>
    <p class="tagline">A task file format for developers working with AI agents.</p>

    <div class="hero-grid">
      <div class="code-block">
        <div class="code-header">brainfile.md</div>
        <pre class="code-content"><span class="code-comment"># YAML structure</span>
---
schema: https://brainfile.md/v1
columns:
  - id: in-progress
    tasks:
      - id: auth-1
        title: Implement OAuth 2.0 authentication
        description: Add Google and GitHub OAuth providers
        priority: high
        tags: [backend, security]
        subtasks:
          - id: auth-1-1
            title: Set up OAuth credentials
            completed: true
          - id: auth-1-2
            title: Implement callback handlers
            completed: false
---

<span class="code-comment"># Markdown content</span>
# Project Tasks

Additional context for AI agents.</pre>
      </div>

      <div class="tool-cards">
        <a href="/core/overview" class="tool-card">
          <div class="tool-card-title">Core Library</div>
          <div class="tool-card-package">@brainfile/core</div>
          <div class="tool-card-description">Parse and validate brainfile format</div>
        </a>

        <a href="/cli/installation" class="tool-card">
          <div class="tool-card-title">CLI Tool</div>
          <div class="tool-card-package">npm i -g @brainfile/cli</div>
          <div class="tool-card-description">Command-line interface for automation</div>
        </a>

        <a href="/vscode/extension" class="tool-card">
          <div class="tool-card-title">VSCode Extension</div>
          <div class="tool-card-package">Visual kanban board</div>
          <div class="tool-card-description">Integrated into your editor</div>
        </a>
      </div>
    </div>

    <ul class="feature-list">
      <li>YAML structure + Markdown content</li>
      <li>Git-friendly text format</li>
      <li>AI agents can read and modify it</li>
    </ul>

    <section>
      <h2>Why this exists</h2>
      
      <div class="two-col">
        <div>
          <div class="col-header">Working with AI agents on tasks is messy</div>
          <ul>
            <li>Paste task lists into prompts</li>
            <li>Lose track of what changed</li>
            <li>No version history</li>
            <li>Context spread across tools</li>
          </ul>
        </div>
        <div>
          <div class="col-header">Brainfile is a file you commit</div>
          <ul>
            <li>Tasks live in your repo</li>
            <li>Agents follow explicit rules</li>
            <li>Full git history</li>
            <li>Tools read the same format</li>
          </ul>
        </div>
      </div>
    </section>

    <div class="footer-statement">
      That's it. It's a file format.
    </div>
  </main>

  <script>
    const themeToggle = document.getElementById('themeToggle');
    const html = document.documentElement;

    function updateThemeIcon(theme) {
      const sunIcon = themeToggle?.querySelector('.sun-icon');
      const moonIcon = themeToggle?.querySelector('.moon-icon');
      if (theme === 'dark') {
        sunIcon.style.display = 'block';
        moonIcon.style.display = 'none';
      } else {
        sunIcon.style.display = 'none';
        moonIcon.style.display = 'block';
      }
    }

    themeToggle?.addEventListener('click', () => {
      const currentTheme = html.getAttribute('data-theme');
      const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
      html.setAttribute('data-theme', newTheme);
      localStorage.setItem('theme', newTheme);
      updateThemeIcon(newTheme);
    });

    const savedTheme = localStorage.getItem('theme') || 'dark';
    html.setAttribute('data-theme', savedTheme);
    updateThemeIcon(savedTheme);
  </script>
</body>
</html>
